apiVersion: apps/v1
kind: Deployment
metadata:
  name: exercise-agent
  namespace: learnflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exercise-agent
  template:
    metadata:
      labels:
        app: exercise-agent
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "exercise-agent"
        dapr.io/app-port: "8000"
    spec:
      containers:
      - name: exercise-agent
        image: exercise-agent:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: exercise-agent
  namespace: learnflow
spec:
  selector:
    app: exercise-agent
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP

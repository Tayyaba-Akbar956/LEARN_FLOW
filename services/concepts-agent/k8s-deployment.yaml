apiVersion: apps/v1
kind: Deployment
metadata:
  name: concepts-agent
  namespace: learnflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: concepts-agent
  template:
    metadata:
      labels:
        app: concepts-agent
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "concepts-agent"
        dapr.io/app-port: "8000"
    spec:
      containers:
      - name: concepts-agent
        image: concepts-agent:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: concepts-agent
  namespace: learnflow
spec:
  selector:
    app: concepts-agent
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
